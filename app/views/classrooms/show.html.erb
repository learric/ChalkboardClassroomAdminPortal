<%= render 'layouts/header_main' %>

<div class="content_overlay content_column">
  <div class="container-fluid content_overlay">

    <h1><%= @classroom.name %></h1>

    <p id="notice"><%= notice %></p>

    <table class="table">
      <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th></th>
      </tr>
      </thead>

      <tbody>
      <tr>
        <td>Student Name</td>
        <td>student@email.com</td>
        <td><%= link_to 'Remove', '#', class: 'btn btn-danger' %></td>
      </tr>
      </tbody>
    </table>

    <%= link_to 'Add Student', '#', class: 'btn btn-primary' %>
    <%= link_to 'Back', classrooms_path, class: 'btn btn-danger' %>
  </div>
</div>
